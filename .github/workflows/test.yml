name: Test CI
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - run: go mod tidy
      - run: go get github.com/mattn/goveralls github.com/sonatype-nexus-community/nancy
      - run: go list -m all | nancy -quiet
      - run: go vet -x .
      - run: go test -covermode=atomic -coverprofile="cover.out" .
